version: '3.8'
services:
  backend:
    build: ./backend
    container_name: simple_backend
    ports:
      - "5000:5000"
  frontend:
    build: ./frontend
    container_name: simple_frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
